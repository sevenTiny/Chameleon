@using Chameleon.Entity;
@using SevenTiny.Bantina.Extensions.AspNetCore;
@{
    ViewData["Title"] = "Add";
    Layout = "~/Views/Shared/_LayoutHeaderStyle.cshtml";

    var result = Model as ResponseModel ?? new ResponseModel();
    var entity = result?.Data as UserAccount ?? new UserAccount();

    List<Organization> organizations = (ViewData["Organization"] as List<Organization>) ?? new List<Organization>(0);
    List<Profile> profiles = (ViewData["Profile"] as List<Profile>) ?? new List<Profile>(0);
}
@{
    ViewData["Level_1"] = "用户管理";
    ViewData["Level_2"] = "新增用户";
}
@await Html.PartialAsync("~/Views/Shared/_PartialNavigation.cshtml")

<article class="page-container">
    <form class="form form-horizontal validate" id="form-article-add" method="post" action="/UserAccount/AddLogic">
        <div class="row cl">
            <label class="form-label col-xs-4 col-sm-2"><span class="c-red">*</span>名称：</label>
            <div class="formControls col-xs-8 col-sm-9">
                <input type="text" class="input-text" value="@entity.Name" placeholder="" name="name" required minLength="2" maxlength="50">
            </div>
        </div>
        <div class="row cl">
            <label class="form-label col-xs-4 col-sm-2"><span class="c-red">*</span>邮箱：</label>
            <div class="formControls col-xs-8 col-sm-9">
                <input type="email" class="input-text" value="@entity.Email" placeholder="" name="Email" required minLength="2" maxlength="50">
            </div>
        </div>
        <div class="row cl">
            <label class="form-label col-xs-4 col-sm-2">手机号：</label>
            <div class="formControls col-xs-8 col-sm-9">
                <input type="tel" class="input-text" value="@entity.Phone" placeholder="" name="Phone" minLength="2" maxlength="50">
            </div>
        </div>
        <div class="row cl">
            <label class="form-label col-xs-4 col-sm-2"><span class="c-red">*</span>组织：</label>
            <div class="formControls col-xs-8 col-sm-9">
                <span class="select-box">
                    <select class="input select" value="0" name="Organization" id="Organization" required>
                        <option value="">--请选择--</option>
                        @foreach (var item in organizations)
                        {
                            <option value="@item.Id">@item.Name</option>
                        }
                    </select>
                </span>
            </div>
        </div>
        <div class="row cl">
            <label class="form-label col-xs-4 col-sm-2"><span class="c-red">*</span>身份：</label>
            <div class="formControls col-xs-8 col-sm-9">
                <span class="select-box">
                    <select class="input select" value="0" name="Profile" id="Profile" required>
                        <option value="">--请选择--</option>
                        @foreach (var item in profiles)
                        {
                            <option value="@item.Id">@item.Name</option>
                        }
                    </select>
                </span>
            </div>
        </div>
        <div class="row cl">
            <div class="col-xs-8 col-sm-9 col-xs-offset-4 col-sm-offset-2">
                <input class="btn btn-primary radius" type="submit" value="&nbsp;&nbsp;提交&nbsp;&nbsp;">
                <button onClick="javascript:history.back(-1);" class="btn btn-default radius" type="button">&nbsp;&nbsp;取消&nbsp;&nbsp;</button>
                @if (!result.IsSuccess)
                {
                    <br /><br /><pre><span class="c-red">@result.Message</span></pre>
                }
            </div>
        </div>
    </form>
</article>

@await Html.PartialAsync("~/Views/Shared/_FooterScripts.cshtml")
@await Html.PartialAsync("~/Views/Shared/_CommonScript.cshtml")
@await Html.PartialAsync("~/Views/Shared/_FormValidate.cshtml")
<!--请在下方写此页面业务相关的脚本-->
<script type="text/javascript">
    $("#Organization").val('@entity.Organization')
    $("#Profile").val('@entity.Profile')
</script>
<!--/请在上方写此页面业务相关的脚本-->